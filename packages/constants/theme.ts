import { MermaidConfig } from 'mermaid'

export const CodeHighlightThemeMap = {
  dark: 'github-dark',
  light: 'github-light',
}
export const CodeMermaidThemeMap: Record<string, MermaidConfig['theme']> = {
  dark: 'dark',
  light: 'default',
}
